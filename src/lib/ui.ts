// Shared "glass" translucency for floating panels (popovers, drawers, menus).
// Kept in one place so every panel stays visually consistent and the
// blur/opacity values only need tuning in one spot.
export const GLASS_PANEL = "bg-background/80 backdrop-blur-2xl";
